import React from "react";

function Cart() {
  return (
    <div
      className="App"
      style={{
        display: "flex",
        flexDirection: "column",
        margin: 10,
        backgroundColor: "#ececec",
        padding: 10,
        width: "20%",
      }}
    >
      Cart
    </div>
  );
}

export default Cart;
